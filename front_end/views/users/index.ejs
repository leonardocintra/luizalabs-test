<% include ../header %>
<h1 v-show="isList">Usuários <a href="/" title="Novo usuário" class="btn btn-default btn-sm">Adicionar</a></h1>
<h1 v-show="isForm">Editar Usuário</h1>

<div class="user-list" v-show="isList">
    <table class="table table-hover">
        <thead>
            <th>Nome</th>
            <th>Usuário</th>
            <th colspan="2">Facebook ID</th>
        </thead>

        <tbody>
            <tr v-repeat="user: query.objects" class="item-{{user.id}}" v-transtition>
                <td><a href="#"  v-on="click: edit(user, $event)">{{user.name}}</a></td>
                <td>{{user.username}}</td>
                <td>{{user.fb_id}}</td>
                <td>
                    <button class="btn btn-link btn-sm" v-on="click: delete(user, $event)"><i class="glyphicon glyphicon-trash"></i></button>
                </td>
            </tr>
        </tbody>
    </table>

    <ul class="pagination">
        <li v-show="query.prev">
            <a href="javascript:void()" v-on="click: list(query.prev)" aria-label="Previous">
                <span aria-hidden="true">&laquo;</span>
            </a>
        </li>
        <li v-repeat="query.pages">
            <a href="javascript:void()" v-on="click:list($index+1)">{{$index+1}}</a>
        </li>
        <li v-show="query.next">
            <a href="javascript:void(0)" v-on="click: list(query.next)" aria-label="Next">
                <span aria-hidden="true">&raquo;</span>
            </a>
        </li>
    </ul>
</div>

<% include ../users/_form %>
<% include ../footer %>
